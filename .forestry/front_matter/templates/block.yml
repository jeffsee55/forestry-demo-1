---
label: Block
hide_body: true
fields:
- name: block
  type: blocks
  label: Block
  template_types:
  - sections
  - sortable-list-empty-values
  - image-gallery
  config:
    min: 
    max: 
pages:
- blocks.md
